{
  "id": "seed-w20",
  "name": "SeedStack W20 integration",
  "security": {
    "provider": "${securityProvider}",
    "config": {
      "authentication": "${seed-rest-path-slash}web-bridge/security/authentication",
      "authorizations": "${seed-rest-path-slash}web-bridge/security/authorizations"
    }
  }
}